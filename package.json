{
  "name": "accru-client",
  "version": "0.0.10",
  "description": "Client SDK for Accru API",
  "main": "index.js",
  "repository": "TBH",
  "author": "Accru Co.",
  "license": "MIT",
  "scripts": {
    "watch": "tsc -w",
    "postwatch": "cp package.json dist/package.json && cd dist && yarn link",
    "start": "concurrently \"npm:watch\" \"npm:postwatch\"",
    "build": "tsc",
    "postbuild": "node postBuild.js",
    "deploy": "yarn build && cd dist && npm publish --access public",
    "sandbox": "yarn link accru-client && nodemon src/tests/sandbox.js"
  },
  "dependencies": {
    "@apollo/client": "^3.8.8",
    "apollo-link": "^1.2.14",
    "dotenv": "^16.3.1",
    "graphql": "^16.8.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2",
    "react": "^18.2.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": "18.x"
  }
}
